<div class="container-fluid">
  <h2>Your Scores at <%= @course.name %></h2>
  <% @rounds.each do |r| %>
    <li><%= r.score %></li>
    <li><%= r.date.strftime("%D") %></li>
  <% end %>
  <br>
  <%= link_to "Go back to your courses", courses_path, class: "btn btn-primary btn-sm" %>
</div>